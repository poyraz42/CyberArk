<UserControl x:Class="VaultDashboard.App.Views.Pages.OverviewPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vm="clr-namespace:VaultDashboard.App.ViewModels.Pages"
             d:DataContext="{d:DesignInstance Type=vm:OverviewViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Vault Overview" Style="{StaticResource SectionHeaderTextStyle}" />

            <UniformGrid Columns="4" Rows="2" Margin="0,0,0,20">
                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="{Binding SafesCount}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Safes" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="{Binding AccountsCount}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Accounts" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,12,12">
                    <StackPanel>
                        <TextBlock Text="{Binding ManagedAccountsPercent, StringFormat={}{0}%}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Managed accounts" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="{Binding NonCompliantAccountsCount}" Style="{StaticResource KpiValueTextStyle}"
                                   Foreground="{StaticResource StatusWarningBrush}" />
                        <TextBlock Text="Non-compliant accounts" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{Binding UsersCount}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Users" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{Binding GroupsCount}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Groups" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{Binding ActivePlatformsCount}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Active platforms" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardPanelStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding LiveSessionsCount}" Style="{StaticResource KpiValueTextStyle}" />
                        <TextBlock Text="Live PSM sessions" Style="{StaticResource KpiLabelTextStyle}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardPanelStyle}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Account compliance" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                        <lvc:PieChart Series="{Binding AccountsComplianceSeries}" Height="220" LegendPosition="Right" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardPanelStyle}">
                    <StackPanel>
                        <TextBlock Text="Managed vs. unmanaged accounts" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                        <lvc:PieChart Series="{Binding AccountsManagedSeries}" Height="220" LegendPosition="Right" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Highest safe retention (top 8, days)" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                    <lvc:CartesianChart Series="{Binding TopSafeRetentionSeries}" XAxes="{Binding TopSafeRetentionXAxes}"
                                         Height="260" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardPanelStyle}">
                <StackPanel>
                    <TextBlock Text="Last refresh: endpoint status" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                    <DataGrid ItemsSource="{Binding FetchLog}" AutoGenerateColumns="False" IsReadOnly="True"
                              MaxHeight="260" GridLinesVisibility="None" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="90" />
                            <DataGridTextColumn Header="Endpoint" Binding="{Binding Endpoint}" Width="240" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Success}" Width="80" />
                            <DataGridTextColumn Header="Items" Binding="{Binding ItemCount}" Width="70" />
                            <DataGridTextColumn Header="Error" Binding="{Binding ErrorMessage}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
