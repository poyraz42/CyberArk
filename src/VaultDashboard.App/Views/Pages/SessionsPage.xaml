<UserControl x:Class="VaultDashboard.App.Views.Pages.SessionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vm="clr-namespace:VaultDashboard.App.ViewModels.Pages"
             d:DataContext="{d:DesignInstance Type=vm:SessionsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Sessions &amp; PSM" Style="{StaticResource SectionHeaderTextStyle}" />

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardPanelStyle}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Active sessions by protocol" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                        <lvc:PieChart Series="{Binding SessionsByProtocolSeries}" Height="220" LegendPosition="Right" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardPanelStyle}">
                    <StackPanel>
                        <TextBlock Text="PSM servers" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                        <DataGrid ItemsSource="{Binding PsmServers}" AutoGenerateColumns="False" IsReadOnly="True"
                                  MaxHeight="200" GridLinesVisibility="None" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Server ID" Binding="{Binding PsmServerId}" Width="140" />
                                <DataGridTextColumn Header="Name" Binding="{Binding PsmServerName}" Width="*" />
                                <DataGridCheckBoxColumn Header="Default" Binding="{Binding IsDefaultPsmServer}" Width="70" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Style="{StaticResource CardPanelStyle}">
                <StackPanel>
                    <TextBlock Text="Active PSM sessions" Style="{StaticResource SubtleTextStyle}" Margin="0,0,0,8" />
                    <DataGrid ItemsSource="{Binding LiveSessions}" AutoGenerateColumns="False" IsReadOnly="True"
                              MaxHeight="420" GridLinesVisibility="None" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="User" Binding="{Binding User}" Width="140" />
                            <DataGridTextColumn Header="Account" Binding="{Binding AccountUsername}" Width="140" />
                            <DataGridTextColumn Header="Safe" Binding="{Binding SafeName}" Width="160" />
                            <DataGridTextColumn Header="Platform" Binding="{Binding PlatformId}" Width="160" />
                            <DataGridTextColumn Header="Target" Binding="{Binding RemoteMachine}" Width="160" />
                            <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="90" />
                            <DataGridTextColumn Header="Started" Binding="{Binding Start}" Width="150" />
                            <DataGridTextColumn Header="Risk" Binding="{Binding RiskScore}" Width="60" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
